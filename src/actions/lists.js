import { createAction } from 'redux-act';

export const addList = createAction('Add list');
export const changeListTitle = createAction('Change list title');
export const removeList = createAction('Remove list');
